# Object Detection Model

This project is an implementation of an SSD (Single Shot Multibox Detector) for object detection. The model is based on a pre-trained ResNet50 backbone and uses the Pascal VOC dataset.

## Setup
- Clone the repository
- Install dependencies using `pip install -r requirements.txt`

## Training
To train the model, run:
```bash
python train.py
